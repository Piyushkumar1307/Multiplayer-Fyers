# Same API as client — see client/.env.example
# npm run dev  → open http://localhost:5174/admin/
# npm run build → assets under /admin/ for combined Netlify deploy
#
# VITE_API_URL=http://localhost:4000
# VITE_SOCKET_URL=http://localhost:4000
