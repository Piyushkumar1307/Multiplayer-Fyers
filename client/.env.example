# Auto-detect (recommended): no file needed, or leave .env empty.
#
# npm run dev     → uses .env.development → localhost:4000
# npm run build   → uses .env.production  → Render API
#
# Optional override in .env:
# VITE_API_URL=http://localhost:4000
# VITE_SOCKET_URL=http://localhost:4000
