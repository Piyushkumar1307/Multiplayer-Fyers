# Used only if Netlify base directory is set to client/ (legacy).
# Preferred: deploy from repo root using /netlify.toml (player + /admin/).

/admin/*  /admin/index.html  200
/*        /index.html        200
